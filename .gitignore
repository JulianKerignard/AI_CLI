node_modules/
*.log
.DS_Store
.env
.env.local

# dist/ est volontairement committé : permet 'npm i -g github:...' sans
# devDependencies (typescript) ni build au install. Regénéré via 'npm run build'.

aicli-*.tgz
